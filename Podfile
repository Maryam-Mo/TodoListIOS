platform :ios, '9.0'

  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  def shared_pods
    pod 'Alamofire'
    pod 'SwiftyJSON'
  # Pods for TodoList
  end
  
  target 'TodoList' do
     # Pods for Modes
     shared_pods
  end
